echo "Program for executing UNIX command using shell programming"
echo "Welcome"
ps
#ps is a command to display the process status
#It provides a snapshot of the current processes, including their process IDs (PIDs), the terminal they are running on, the time they have been running, and the command that started them.